Repair Commander at commit `c3ffcfcdac9237cb446ae0acc5b228380e6ba52a` so valid negative scientific-notation command arguments using uppercase `E` are recognized exactly like lowercase `e` instead of being rejected as unknown options. Malformed numeric-looking options must remain rejected, and digit option flags must retain precedence. Keep the production patch minimal. Do not change tests, fixtures, dependencies, evaluation controls, or audit artifacts. Run the locked acceptance harness and the full regression/type command with the host's inherited `NO_COLOR` temporarily removed: `$round2NoColor=$env:NO_COLOR; Remove-Item Env:NO_COLOR -ErrorAction SilentlyContinue; try { npm test } finally { if ($null -ne $round2NoColor) { $env:NO_COLOR=$round2NoColor } }`. Report exact commands, results, scope, and risks.
