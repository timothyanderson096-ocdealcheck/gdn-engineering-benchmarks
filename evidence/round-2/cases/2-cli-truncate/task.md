Repair `cli-truncate` at commit `2af3e232c8503d29bd81cb86c6a664721936fa0a` so `preferTruncationOnSpace` at `position: 'start'` or `'end'` reserves the full display width of a custom multi-column truncation character. The returned display width must never exceed `columns`. Preserve ANSI styling, wide-character handling, default marker behavior, and other positions. Keep the production patch minimal. Do not change tests, fixtures, dependencies, evaluation controls, or audit artifacts. Run the locked acceptance harness plus `npx xo index.js test.js`, `npx ava`, and `npx tsd`; report exact commands, results, scope, and risks. (`npm test` is not the locked regression command because current XO project-service parsing of the two declaration files fails independently on this host.)
