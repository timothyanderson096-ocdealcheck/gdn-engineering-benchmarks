Repair `query-string` at commit `2e1f45aafb71ef247572b10d9d37dce67cd825ac` so that, with `arrayFormat: 'separator'`, only a literal separator in the raw query value creates array elements. A percent-encoded separator is data within an element. Preserve existing behavior and keep the source patch minimal. Do not change tests, fixtures, dependencies, the evaluation folder, or benchmark controls. Run the direct acceptance harness and the repository regression/type checks, and report commands and remaining risks.
