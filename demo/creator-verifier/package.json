{
  "name": "gdn-creator-verifier-evidence-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local explanation of the published GDN benchmark evidence.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node --check src/app.js && node --check src/evidence.mjs && node --check scripts/build.mjs && node --check scripts/serve.mjs",
    "test": "npm run build && npm run lint && node --test tests/rendered-output.test.mjs tests/interaction.test.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "start": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
