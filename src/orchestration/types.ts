export type AgentRole = "PLANNER" | "BUILDER" | "ADVERSARIAL_REVIEWER" | "VERIFIER" | "EVIDENCE_JUDGE" | "REPAIRER" | "SPECIALIST" | "OTHER";
export type RunStatus = "PENDING" | "COMPLETED" | "FAILED";
export type DisagreementStatus = "OPEN" | "PARTIALLY_RESOLVED" | "RESOLVED_BY_TEST" | "RESOLVED_BY_EVIDENCE" | "ACCEPTED_UNCERTAINTY";
export type VerificationStatus = "PENDING" | "PASSED" | "FAILED" | "INCONCLUSIVE";
export type JudgmentStatus = "SUPPORTED" | "PARTIALLY_SUPPORTED" | "UNSUPPORTED" | "CONTRADICTED" | "INSUFFICIENT_EVIDENCE";
export type SessionStatus = "PLANNED" | "PROPOSED" | "REPAIR_REQUIRED" | "VERIFIED" | "UNRESOLVED";

export interface EngineeringObjective { id: string; statement: string; constraints: readonly string[]; acceptanceCriteria: readonly string[]; taskTags: readonly string[]; domainTags: readonly string[]; contextRequirement: number; }
export interface EngineeringTask { id: string; objectiveId: string; title: string; responsibility: string; requiredRole: AgentRole; dependencies: readonly string[]; verificationRequirementIds: readonly string[]; }
export interface ModelIdentity { modelId: string; providerAdapterId?: string; displayName?: string; }
export interface AgentCandidate { agentId: string; model: ModelIdentity; supportedRoles: readonly AgentRole[]; taskTags: readonly string[]; domainTags: readonly string[]; contextLimit: number; notes: readonly string[]; }
export interface AgentAssignment { assignmentId: string; taskId: string; role: AgentRole; agentId: string; modelId: string; routingDecisionId: string; responsibility: string; }
export interface AgentClaim { claimId: string; runId: string; statement: string; affectedRequirementIds: readonly string[]; supportingEvidenceIds: readonly string[]; confidence: number; }
export interface AgentRun { runId: string; assignmentId: string; role: AgentRole; agentId: string; modelId: string; iteration: number; status: RunStatus; startedAt: string; completedAt?: string; outputSummary: string; claims: readonly AgentClaim[]; artifactReferences: readonly string[]; latencyMs?: number; costUnits?: number; }
export interface AgentDisagreement { disagreementId: string; agentIds: readonly string[]; modelIds: readonly string[]; claimA: AgentClaim; claimB: AgentClaim; affectedRequirementId: string; evidenceSupportingA: readonly string[]; evidenceSupportingB: readonly string[]; verificationNeeded: string; status: DisagreementStatus; resolution?: string; confidence: number; notes: readonly string[]; }
export type VerificationMethod = "AUTOMATED_TEST" | "TYPECHECK" | "BUILD" | "STATIC_ANALYSIS" | "DETERMINISTIC_REPRODUCTION" | "BENCHMARK" | "OBSERVABLE_BEHAVIOR" | "REQUIREMENT_CHECK" | "ARTIFACT_COMPARISON" | "OTHER";
export interface VerificationRequirement { requirementId: string; statement: string; method: VerificationMethod; observablePassCondition: string; machineVerifiable: boolean; }
export interface VerificationEvidence { evidenceId: string; requirementId: string; method: VerificationMethod; commandOrProcedure: string; observedResult: string; artifactReferences: readonly string[]; timestamp: string; }
export interface VerificationResult { verificationId: string; iteration: number; requirementId: string; status: VerificationStatus; evidence: readonly VerificationEvidence[]; verifierRunId: string; notes: readonly string[]; }
export interface EvidenceJudgment { judgmentId: string; iteration: number; claimId: string; status: JudgmentStatus; verificationEvidenceIds: readonly string[]; rationale: string; unresolvedDisagreementIds: readonly string[]; judgeRunId: string; }
export interface RepairIteration { iteration: number; triggeredByVerificationIds: readonly string[]; triggeredByDisagreementIds: readonly string[]; originalArtifactReferences: readonly string[]; repairedArtifactReferences: readonly string[]; repairRunId: string; verificationResultIds: readonly string[]; outcome: "PENDING" | "RESOLVED" | "FAILED"; }
export interface EngineeringOutcome { status: SessionStatus; resultSummary: string; verifiedRequirementIds: readonly string[]; unresolvedRequirementIds: readonly string[]; finalArtifactReferences: readonly string[]; completedAt: string; }
export interface ReusableEngineeringLesson { lessonId: string; statement: string; applicableTaskTags: readonly string[]; applicableRoles: readonly AgentRole[]; evidenceReferences: readonly string[]; modelNeutral: true; notes: readonly string[]; }
export type CapabilityObservationKind = "VALID_DECOMPOSITION" | "FIRST_PASS_VERIFIED" | "MATERIAL_DEFECT_FOUND" | "FALSE_POSITIVE_NOISE" | "MISSED_DEFECT_CAUGHT" | "UNSUPPORTED_CONSENSUS_OVERRULED" | "REPAIR_SUCCEEDED" | "ROUTING_EFFECTIVE" | "ROUTING_INEFFECTIVE" | "AVOIDABLE_REWORK";
export interface CapabilityObservation { observationId: string; modelId: string; agentId: string; role: AgentRole; taskTags: readonly string[]; domainTags: readonly string[]; kind: CapabilityObservationKind; successful: boolean; verifiedByEvidenceIds: readonly string[]; latencyMs?: number; costUnits?: number; timestamp: string; notes: readonly string[]; }
export interface OrchestrationSession { sessionId: string; objective: EngineeringObjective; tasks: readonly EngineeringTask[]; assignments: readonly AgentAssignment[]; routingHistory: readonly RoutingDecision[]; runs: readonly AgentRun[]; disagreements: readonly AgentDisagreement[]; verificationRequirements: readonly VerificationRequirement[]; verificationResults: readonly VerificationResult[]; evidenceJudgments: readonly EvidenceJudgment[]; repairIterations: readonly RepairIteration[]; outcome: EngineeringOutcome; capabilityObservations: readonly CapabilityObservation[]; reusableLessons: readonly ReusableEngineeringLesson[]; }

export interface RolePerformance { role: AgentRole; taskTags: readonly string[]; domainTags: readonly string[]; sampleSize: number; successfulObservations: number; reliability: number; verificationPassRate: number; disagreementUsefulness: number; repairSuccessRate: number; averageLatencyMs?: number; averageCostUnits?: number; confidenceInEstimate: number; failureModes: readonly string[]; lastUpdated: string; notes: readonly string[]; }
export interface CapabilityProfile { model: ModelIdentity; supportedRoles: readonly AgentRole[]; taskCapabilities: readonly RolePerformance[]; contextLimit: number; notes: readonly string[]; }
export interface CapabilityRegistry { profiles: readonly CapabilityProfile[]; observations: readonly CapabilityObservation[]; }
export interface RoutingConstraints { role: AgentRole; taskTags: readonly string[]; domainTags: readonly string[]; requiredContext: number; minimumSampleSize: number; maximumLatencyMs?: number; maximumCostUnits?: number; }
export interface RoutingCandidateEvaluation { modelId: string; eligible: boolean; matchedTaskTags: readonly string[]; matchedDomainTags: readonly string[]; rolePerformanceSampleSize: number; reliability: number | null; uncertainty: string; exclusions: readonly string[]; }
export interface RoutingDecision { routingDecisionId: string; constraints: RoutingConstraints; selectedModelId?: string; rationale: readonly string[]; evidenceReferences: readonly string[]; uncertainty: readonly string[]; alternatives: readonly RoutingCandidateEvaluation[]; }

export interface EngineeringAgentAdapter { adapterId: string; providerKind: string; supports(model: ModelIdentity): boolean; execute(request: AgentExecutionRequest): Promise<AgentExecutionResponse>; }
export interface AgentExecutionRequest { sessionId: string; task: EngineeringTask; assignment: AgentAssignment; inputs: Readonly<Record<string, unknown>>; }
export interface AgentExecutionResponse { outputSummary: string; claims: readonly Omit<AgentClaim, "runId">[]; artifactReferences: readonly string[]; providerMetadata?: Readonly<Record<string, unknown>>; }
